# Heap snapshots are hundreds of MB each. Keep them locally while you analyse
# them (perf/bin/analyze-heapsnapshot.mjs), commit the analysis, not the dump.
*.heapsnapshot

# Contain live session cookies and API keys for the load-test orgs. The glob
# matters: the multi-tenancy tests add fixtures-b.json and friends.
results/fixtures*.json
